#include <iostream>
#include <cstdio>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    int num;
    scanf("%d", &num);
    vector<vector<int> > number;
    for(int i = 0; i < size; i++) {
        int temp;
        scanf("%d", &temp);
        number.push_back(temp);
    }